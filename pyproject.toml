[project]
name = "macos-world"
version = "0.1.0"
description = "Internal benchmarking platform built on top of the MacOSWorld datasets."
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["infra/cli"]
